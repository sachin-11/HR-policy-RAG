# RAG and agent dependencies.
#
# Install this file later when working on Module 4+:
#
#   python -m pip install -r requirements-rag.txt
#
# `chromadb` may require native build tooling on some Windows setups because of
# `chroma-hnswlib`. If that happens, we can switch to FAISS or use a newer
# compatible Chroma version during the vector-store module.

langchain==0.3.13
langchain-community==0.3.13
langchain-openai==0.2.14
langgraph==0.2.60
chromadb==0.5.23
openai==1.58.1
pinecone==5.4.2
tiktoken==0.8.0
