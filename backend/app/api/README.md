# API Module

FastAPI routes and request/response schemas will live here.

Planned files:

- `chat_routes.py`
- `document_routes.py`
- `schemas.py`

Current files:

- `chat_routes.py` exposes `POST /chat` and versioned `POST /api/v1/chat`.
- `schemas.py` contains shared API response schemas.
